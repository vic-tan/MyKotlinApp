<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.tanlifei.mykotlinapp">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:name=".BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:replace="android:label,networkSecurityConfig"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="com.desay_svautomotive.ACTION_CAR_SETTINGS_WIDGET_STATUS_SWITCH" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.RecyclerActivity" />
        <activity android:name=".activity.CategoryActivity" />
        <activity android:name=".activity.EventActivity" />
        <activity android:name=".activity.EventSendActivity" />
        <activity android:name=".activity.BaseAdapterRecyclerActivity" />
        <activity android:name=".activity.SingleAdapterItemActivity" />
        <activity android:name=".activity.MultiAdapterItemActivity" />
        <meta-data
            android:name="design_width_in_px"
            android:value="1080"/>
        <meta-data
            android:name="design_height_in_px"
            android:value="1920"/>
    </application>

</manifest>