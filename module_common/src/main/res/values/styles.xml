<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/white</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>


    <!-- 个人中心列表样式 -->
    <style name="com_btn_style">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">50dp</item>
        <item name="android:layout_gravity">center</item>
        <item name="android:gravity">center</item>
        <item name="android:textSize">18sp</item>
        <item name="android:textColor">@color/white</item>
        <item name="com.tanlifei.app:background_normal">@color/theme_color</item>
        <item name="com.tanlifei.app:background_pressed">@color/com_C0F2323A</item>
        <item name="com.tanlifei.app:corner_radius">25dp</item>
        <item name="android:orientation">horizontal</item>
    </style>
</resources>
